[[source]]
url = "https://mirrors.aliyun.com/pypi/simple/"
verify_ssl = true
name = "pip_conf_index_global"

[packages]
# data pipeline
pandas = '*'
numpy = '*'
openpyxl = '*'

# utils
fastapi = '*'
pydantic = '*'

# LLMs warehouse
ollama-python = '*'

# PDF processing
magic-pdf = '*'
python-magic = '*'

[requires]
python_version = "3.9"
